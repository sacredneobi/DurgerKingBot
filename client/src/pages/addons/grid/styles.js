const rootBox = {
  padding: (theme) => theme.spacing(0.75),
  margin: (theme) => theme.spacing(0.75),
};

const containerButtonIconDefault = { fontSize: 35 };

export { rootBox, containerButtonIconDefault };
