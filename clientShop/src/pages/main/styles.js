const root = {
  display: "flex",
  flexDirection: "column",
  height: "100vh",
  justifyContent: "space-between",
};

const grid = { overflow: "auto" };
const baseLine = { flexGrow: 1 };

export { root, grid, baseLine };
